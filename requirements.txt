# Core Framework
fastapi==0.139.2
uvicorn[standard]==0.51.0
langchain==1.3.14
langchain-openai==1.3.5
pydantic==2.13.4
# Imported directly by weebot.config.settings; also required transitively by
# mcp/fastapi — pin explicitly so a transitive change can't drop it.
pydantic-settings>=2.5.2
python-dotenv==1.2.2
import-linter==2.13
structlog==26.1.0
prometheus-client>=0.25.0

# Discord Integration
pynacl>=1.5.0

# Web Navigation
# Held at 0.11.13: browser-use 0.13.6 (dependabot #18) hard-pins
# aiohttp==3.13.4 and anthropic==0.76.0, which conflict with the aiohttp
# 3.14.1 / anthropic 0.117.0 bumps in that same group. 0.11.13 uses ranges
# (aiohttp>=3.13.3, anthropic<1.0,>=0.72.1) that accept both.
browser-use==0.11.13
playwright==1.61.0

# Windows Integration
mss==10.2.0
pystray>=0.19.0; platform_system=="Windows"
keyboard>=0.13.5; platform_system=="Windows"
pywin32==311; platform_system=="Windows"
wmi>=1.5.1; platform_system=="Windows"
winotify==1.1.0; platform_system=="Windows"

# Research & Scientific
numpy==2.5.1
pandas==3.0.3
matplotlib==3.11.0
seaborn==0.13.2
scipy==1.18.0
# Imported directly by image tools (ocr, screen_tool, computer_use, som_renderer);
# also pulled by matplotlib — pin explicitly so it can't be dropped transitively.
Pillow>=10.0.0

# Document Processing
pypandoc==1.17
python-docx==1.2.0
openpyxl==3.1.5

# AI/ML
openai>=2.26.0
anthropic==0.117.0
tiktoken==0.13.0

# Data Validation & Types
marshmallow==4.3.0
cerberus>=1.3.4

# HTTP & Networking
aiohttp==3.14.1
requests>=2.34.2
urllib3==2.7.0
charset-normalizer==3.4.9
httpx==0.28.1

# Database & Storage
aiosqlite==0.22.1
asyncpg==0.31.0
alembic>=1.14.0

# CLI & UI
click==8.4.2
rich==14.3.3
prompt-toolkit>=3.0.0
colorama==0.4.6

# Async
aiofiles==25.1.0

# Observability — OpenTelemetry (optional; no-op when not installed)
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
anyio==4.14.2

# Web search & image search
ddgs>=9.0
duckduckgo-search>=7.0

# Utilities
pyyaml>=6.0.3
toml==0.10.2
python-dateutil==2.9.0.post0
uuid-utils>=0.0.0

# Optional: For Jupyter integration
# ipywidgets>=8.0.0
# IPython>=8.0.0

# Optional: For Zotero integration
# pyzotero>=1.5.0

# Filesystem watching (behavior_tracker)
watchdog==6.0.0

# MCP Server
mcp==1.28.1

# Video ingestion
youtube-transcript-api>=1.2.4
yt-dlp>=2024.12.0  # YouTube video download

# Security
cryptography==49.0.0

# Templating
jinja2>=3.1.6

# Caching / Message Queue
valkey==6.1.1  # Drop-in redis replacement (BSD-3-Clause, Linux Foundation)

# Scheduling
apscheduler>=3.10.0

# Testing
pytest==8.4.2
pytest-asyncio==0.24.0
pytest-cov==7.1.0
pytest-mock==3.15.1
pytest-timeout==2.4.0
